//Made by Aniket Chavan on the 24th of March (chavaniket@outlook.com)
//reads two positive integers and prints all numbers in between them

#include <stdio.h>

int main(void) {
    int lower;
    int upper;
    
    printf("Enter lower: ");
    scanf("%d", &lower);
    
    printf("Enter upper: ");
    scanf("%d", &upper);
    
    for(int i = lower + 1;i < upper; i++) {
        printf("%d\n",i);
    }
}
